<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.chenweijiang.wcg_mall.mapper.OrderDetailMapper">


    <select id="getByOrderNumber" resultType="com.chenweijiang.wcg_mall.pojo.vo.OrderDetailProductList">
        select p.id as id,p.name as productName ,od.product_number as productNumber ,
               p.price as productPrice,p.image as productImage
        from order_detail as od,product as p
        where od.product_id = p.id and od.order_number = #{orderNumber}
    </select>
    <select id="getOrderDetailList" resultType="com.chenweijiang.wcg_mall.pojo.vo.AdminOrderDetailVO">
        select o.id as id,
               o.order_number as orderNumber,
               o.product_id as productId,
               o.product_number as productNumber,
               p.name as productName,
               p.price as productPrice,
               p.image as productImage
        from order_detail as o,product as p
        where o.product_id = p.id
    </select>


</mapper>
